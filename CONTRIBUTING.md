# Contributing

1. Fork the repository and create a focused branch.
2. Keep all user-facing text in English and all files in UTF-8.
3. Run `bash tests/test.sh` before opening a pull request.
4. Explain user-visible changes in `CHANGELOG.md`.
5. Do not update pinned versions without validating a clean install and update.
